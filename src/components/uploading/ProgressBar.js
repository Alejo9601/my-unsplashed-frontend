import { MotionBar, StaticBar } from "../../styles/styled/div";

const ProgressBar = () => {
   return (
      <StaticBar>
         <MotionBar />
      </StaticBar>
   );
};

export default ProgressBar;
